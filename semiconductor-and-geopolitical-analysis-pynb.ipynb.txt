{"metadata":{"kernelspec":{"language":"python","display_name":"Python 3","name":"python3"},"language_info":{"name":"python","version":"3.12.13","mimetype":"text/x-python","codemirror_mode":{"name":"ipython","version":3},"pygments_lexer":"ipython3","nbconvert_exporter":"python","file_extension":".py"},"kaggle":{"accelerator":"none","dataSources":[],"dockerImageVersionId":28755,"isInternetEnabled":false,"language":"python","sourceType":"notebook","isGpuEnabled":false}},"nbformat_minor":4,"nbformat":4,"cells":[{"cell_type":"code","source":"import pandas as pd\n\nprices_df = pd.read_csv('/kaggle/input/datasets/sergionefedov/global-semiconductor-industry-2010-2026/chip_prices.csv')\ncontrols_df = pd.read_csv('/kaggle/input/datasets/sergionefedov/global-semiconductor-industry-2010-2026/export_controls.csv')\n\nprint(\"Prices Columns:\", prices_df.columns.tolist())\nprint(\"Controls Columns:\", controls_df.columns.tolist())\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-07-13T06:18:47.350403Z","iopub.execute_input":"2026-07-13T06:18:47.351446Z","iopub.status.idle":"2026-07-13T06:18:47.373099Z","shell.execute_reply.started":"2026-07-13T06:18:47.351401Z","shell.execute_reply":"2026-07-13T06:18:47.371731Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# 2. Standardize both into matching 'YYYY-MM' string columns\nprices_df['year_month'] = pd.to_datetime(prices_df['year_month']).dt.strftime('%Y-%m')\ncontrols_df['year_month'] = pd.to_datetime(controls_df['date']).dt.strftime('%Y-%m')\n\n# 3. Print verification\nprint(\"Prices data sample:\")\nprint(prices_df[['year_month', 'product', 'price']].head(2))\n\nprint(\"\\nControls data sample:\")\nprint(controls_df[['year_month', 'policy_name', 'severity_score']].head(2))","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-07-13T06:19:10.664252Z","iopub.execute_input":"2026-07-13T06:19:10.664516Z","iopub.status.idle":"2026-07-13T06:19:10.678827Z","shell.execute_reply.started":"2026-07-13T06:19:10.664496Z","shell.execute_reply":"2026-07-13T06:19:10.677829Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# 1. Aggregate control events by month\nmonthly_controls = controls_df.groupby('year_month').agg({\n    'description': lambda x: ' '.join(x.astype(str)),\n    'severity_score': 'max'\n}).reset_index()\n\n# Rename columns to reflect the monthly aggregation\nmonthly_controls.columns = ['year_month', 'monthly_policy_text', 'max_severity']\n\n# 2. Merge with the pricing data\nmerged_df = pd.merge(prices_df, monthly_controls, on='year_month', how='left')\n\n# 3. Handle months with no political events\nmerged_df['monthly_policy_text'] = merged_df['monthly_policy_text'].fillna('No event')\nmerged_df['max_severity'] = merged_df['max_severity'].fillna(0)\n\nprint(\"Merged Dataframe Shape:\", merged_df.shape)\nprint(\"\\nSample rows where a policy event occurred:\")\nprint(merged_df[merged_df['max_severity'] > 0][['year_month', 'product', 'price', 'max_severity']].head(3))\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-07-13T06:20:31.869886Z","iopub.execute_input":"2026-07-13T06:20:31.870164Z","iopub.status.idle":"2026-07-13T06:20:31.900354Z","shell.execute_reply.started":"2026-07-13T06:20:31.870141Z","shell.execute_reply":"2026-07-13T06:20:31.899097Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# 1. Ensure data is ordered correctly by time within each product group\nmerged_df = merged_df.sort_values(['product', 'year_month']).reset_index(drop=True)\n\n# 2. Create 1-month and 2-month price lags per product\nmerged_df['price_lag_1'] = merged_df.groupby('product')['price'].shift(1)\nmerged_df['price_lag_2'] = merged_df.groupby('product')['price'].shift(2)\n\n# 3. Create a 3-month rolling average of historical prices\n# We use closed='left' to ensure the current month's price isn't included in its own history\nmerged_df['price_rolling_mean_3m'] = merged_df.groupby('product')['price'].transform(\n    lambda x: x.shift(1).rolling(window=3, min_periods=1).mean()\n)\n\n# 4. Drop the first couple of rows per product that now contain NaN values due to shifting\ncleaned_ml_df = merged_df.dropna(subset=['price_lag_1', 'price_lag_2']).reset_index(drop=True)\n\nprint(\"Features created successfully. Current matrix shape:\", cleaned_ml_df.shape)\nprint(\"\\nCheck lagging for a single product sample:\")\nprint(cleaned_ml_df[['year_month', 'product', 'price', 'price_lag_1', 'price_rolling_mean_3m']].head(3))\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-07-13T06:24:09.289883Z","iopub.execute_input":"2026-07-13T06:24:09.290139Z","iopub.status.idle":"2026-07-13T06:24:09.309752Z","shell.execute_reply.started":"2026-07-13T06:24:09.290117Z","shell.execute_reply":"2026-07-13T06:24:09.308774Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"from sklearn.feature_extraction.text import TfidfVectorizer\nimport scipy.sparse as sp\nimport numpy as np\n\n# 1. Vectorize the NLP text columns (Extracting top 100 phrase concepts)\ntfidf = TfidfVectorizer(max_features=100, stop_words='english')\nX_text_sparse = tfidf.fit_transform(cleaned_ml_df['monthly_policy_text'])\n\n# 2. One-Hot Encode the categorical 'product' column\nX_product_dummies = pd.get_dummies(cleaned_ml_df['product'], drop_first=True)\n\n# 3. Gather our core numeric features\nX_numeric = cleaned_ml_df[['max_severity', 'price_lag_1', 'price_lag_2', 'price_rolling_mean_3m']].values\n\n# 4. Combine text, categorical, and numeric features into a single matrix\nX_combined = np.hstack([X_text_sparse.toarray(), X_product_dummies.values, X_numeric])\ny = cleaned_ml_df['price'].values\n\n# 5. Chronological Train-Test Split (e.g., use last 20% of time for testing)\nsplit_idx = int(len(cleaned_ml_df) * 0.8)\n\nX_train, X_test = X_combined[:split_idx], X_combined[split_idx:]\ny_train, y_test = y[:split_idx], y[split_idx:]\n\nprint(\"Final Training features shape:\", X_train.shape)\nprint(\"Final Testing features shape:\", X_test.shape)\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-07-13T06:25:55.216184Z","iopub.execute_input":"2026-07-13T06:25:55.216453Z","iopub.status.idle":"2026-07-13T06:25:55.914091Z","shell.execute_reply.started":"2026-07-13T06:25:55.216431Z","shell.execute_reply":"2026-07-13T06:25:55.912686Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"from sklearn.ensemble import RandomForestRegressor\nfrom sklearn.metrics import mean_absolute_error, r2_score\n\n# 1. Initialize and train the model\nmodel = RandomForestRegressor(n_estimators=100, random_state=42, n_jobs=-1)\nmodel.fit(X_train, y_train)\n\n# 2. Predict on the unseen test set\ny_pred = model.predict(X_test)\n\n# 3. Calculate production performance metrics\nmae = mean_absolute_error(y_test, y_pred)\nr2 = r2_score(y_test, y_pred)\n\nprint(f\"Mean Absolute Error (MAE): ${mae:.2f}\")\nprint(f\"R² Score (Variance Explained): {r2:.4f}\")\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-07-13T06:27:49.433792Z","iopub.execute_input":"2026-07-13T06:27:49.434194Z","iopub.status.idle":"2026-07-13T06:27:50.111860Z","shell.execute_reply.started":"2026-07-13T06:27:49.434169Z","shell.execute_reply":"2026-07-13T06:27:50.110974Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"from sklearn.feature_extraction.text import TfidfVectorizer\nimport numpy as np\nimport pandas as pd\n\n# FIX: Sort globally by time so the index split cuts by TIME, not by product blocks\ncleaned_ml_df = cleaned_ml_df.sort_values('year_month').reset_index(drop=True)\n\n# 1. Vectorize the NLP text columns\ntfidf = TfidfVectorizer(max_features=100, stop_words='english')\nX_text_sparse = tfidf.fit_transform(cleaned_ml_df['monthly_policy_text'])\n\n# 2. One-Hot Encode the categorical 'product' column\nX_product_dummies = pd.get_dummies(cleaned_ml_df['product'], drop_first=True)\n\n# 3. Gather core numeric features\nX_numeric = cleaned_ml_df[['max_severity', 'price_lag_1', 'price_lag_2', 'price_rolling_mean_3m']].values\n\n# 4. Combine text, categorical, and numeric features\nX_combined = np.hstack([X_text_sparse.toarray(), X_product_dummies.values, X_numeric])\ny = cleaned_ml_df['price'].values\n\n# 5. Chronological Train-Test Split (Now it correctly splits past vs future)\nsplit_idx = int(len(cleaned_ml_df) * 0.8)\n\nX_train, X_test = X_combined[:split_idx], X_combined[split_idx:]\ny_train, y_test = y[:split_idx], y[split_idx:]\n\nprint(\"Fixed Training shape:\", X_train.shape)\nprint(\"Fixed Testing shape:\", X_test.shape)\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-07-13T06:30:08.341704Z","iopub.execute_input":"2026-07-13T06:30:08.342176Z","iopub.status.idle":"2026-07-13T06:30:08.358585Z","shell.execute_reply.started":"2026-07-13T06:30:08.342152Z","shell.execute_reply":"2026-07-13T06:30:08.357667Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"from sklearn.ensemble import RandomForestRegressor\nfrom sklearn.metrics import mean_absolute_error, r2_score\n\n# 1. Initialize and train the model on chronological past data\nmodel = RandomForestRegressor(n_estimators=100, random_state=42, n_jobs=-1)\nmodel.fit(X_train, y_train)\n\n# 2. Predict on the chronological future test set\ny_pred = model.predict(X_test)\n\n# 3. Calculate production metrics\nmae = mean_absolute_error(y_test, y_pred)\nr2 = r2_score(y_test, y_pred)\n\nprint(f\"Fixed Mean Absolute Error (MAE): ${mae:.2f}\")\nprint(f\"Fixed R² Score: {r2:.4f}\")\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-07-13T06:31:48.043085Z","iopub.execute_input":"2026-07-13T06:31:48.044278Z","iopub.status.idle":"2026-07-13T06:31:48.256199Z","shell.execute_reply.started":"2026-07-13T06:31:48.044230Z","shell.execute_reply":"2026-07-13T06:31:48.255116Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# 1. Reconstruct feature names map\ntext_feature_names = [f\"nlp_concept_{i}\" for i in range(100)]\nproduct_feature_names = list(X_product_dummies.columns)\nnumeric_feature_names = ['max_severity', 'price_lag_1', 'price_lag_2', 'price_rolling_mean_3m']\n\nall_feature_names = text_feature_names + product_feature_names + numeric_feature_names\n\n# 2. Get importance values from the trained Random Forest\nimportances = model.feature_importances_\n\n# 3. Pair names with scores and sort them\nfeature_imp_df = pd.DataFrame({\n    'Feature': all_feature_names,\n    'Importance': importances\n}).sort_values(by='Importance', ascending=False).reset_index(drop=True)\n\nprint(\"Top 10 Most Important Features:\")\nprint(feature_imp_df.head(10))\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-07-13T06:33:41.173328Z","iopub.execute_input":"2026-07-13T06:33:41.173635Z","iopub.status.idle":"2026-07-13T06:33:41.206640Z","shell.execute_reply.started":"2026-07-13T06:33:41.173613Z","shell.execute_reply":"2026-07-13T06:33:41.205980Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# Get the vocabulary words matching the TF-IDF array indices\nvocab = tfidf.get_feature_names_out()\n\nprint(\"TOP GEOPOLITICAL TEXT SIGNALS:\")\nprint(f\"nlp_concept_1  -> '{vocab[1]}'\")\nprint(f\"nlp_concept_96 -> '{vocab[96]}'\")\nprint(f\"nlp_concept_49 -> '{vocab[49]}'\")\nprint(f\"nlp_concept_36 -> '{vocab[36]}'\")\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-07-13T06:35:38.012800Z","iopub.execute_input":"2026-07-13T06:35:38.013091Z","iopub.status.idle":"2026-07-13T06:35:38.018519Z","shell.execute_reply.started":"2026-07-13T06:35:38.013067Z","shell.execute_reply":"2026-07-13T06:35:38.017523Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"import matplotlib.pyplot as plt\nimport seaborn as sns\n\n# Set clean, professional styling\nsns.set_theme(style=\"whitegrid\")\nplt.figure(figsize=(10, 6))\n\n# Plot actual vs predicted prices\nsns.scatterplot(x=y_test, y=y_pred, alpha=0.7, color='#1f77b4', edgecolor='w', s=80)\n\n# Add a 45-degree perfect-prediction reference line\nmax_val = max(max(y_test), max(y_pred))\nmin_val = min(min(y_test), min(y_pred))\nplt.plot([min_val, max_val], [min_val, max_val], color='red', linestyle='--', lw=2, label='Perfect Prediction')\n\n# Use log scale to cleanly show both cheap DRAM and expensive GPUs side-by-side\nplt.xscale('log')\nplt.yscale('log')\n\nplt.xlabel('Actual Price ($ - Log Scale)', fontsize=12)\nplt.ylabel('Predicted Price ($ - Log Scale)', fontsize=12)\nplt.title('Geopolitically-Aware Price Predictor: Actual vs. Predicted', fontsize=14, fontweight='bold')\nplt.legend()\nplt.tight_layout()\n\n# Save the plot image to your Kaggle working directory\nplt.savefig('semiconductor_price_predictions.png', dpi=300)\nplt.show()\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-07-13T06:37:19.280459Z","iopub.execute_input":"2026-07-13T06:37:19.280775Z","iopub.status.idle":"2026-07-13T06:37:20.607987Z","shell.execute_reply.started":"2026-07-13T06:37:19.280753Z","shell.execute_reply":"2026-07-13T06:37:20.606556Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"import pandas as pd\n\n# Load the new datasets\nai_market_df = pd.read_csv('/kaggle/input/datasets/sergionefedov/global-semiconductor-industry-2010-2026/ai_chip_market.csv')\nfinancials_df = pd.read_csv('/kaggle/input/datasets/sergionefedov/global-semiconductor-industry-2010-2026/chip_companies_financials.csv')\nfab_capacity_df = pd.read_csv('/kaggle/input/datasets/sergionefedov/global-semiconductor-industry-2010-2026/fab_capacity.csv')\n\nprint(\"--- AI Chip Market Columns ---\")\nprint(ai_market_df.columns.tolist())\nprint(ai_market_df.head(1))\n\nprint(\"\\n--- Companies Financials Columns ---\")\nprint(financials_df.columns.tolist())\nprint(financials_df.head(1))\n\nprint(\"\\n--- Fab Capacity Columns ---\")\nprint(fab_capacity_df.columns.tolist())\nprint(fab_capacity_df.head(1))\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-07-13T06:53:12.158262Z","iopub.execute_input":"2026-07-13T06:53:12.158577Z","iopub.status.idle":"2026-07-13T06:53:12.194741Z","shell.execute_reply.started":"2026-07-13T06:53:12.158523Z","shell.execute_reply":"2026-07-13T06:53:12.193692Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"import numpy as np\nimport pandas as pd\nfrom sklearn.feature_extraction.text import TfidfVectorizer\n\n# 1. Aggregate New Supply Indicators (Fab Capacity)\nannual_fab = fab_capacity_df.groupby('year').agg({\n    'monthly_wafer_capacity': 'sum'\n}).reset_index().rename(columns={'monthly_wafer_capacity': 'global_wafer_capacity'})\n\n# 2. Aggregate New Demand Indicators (AI Market)\nannual_ai = ai_market_df.groupby('year').agg({\n    'estimated_shipments_units': 'sum',\n    'estimated_revenue_usd_m': 'sum'\n}).reset_index().rename(columns={\n    'estimated_shipments_units': 'global_ai_shipments',\n    'estimated_revenue_usd_m': 'global_ai_revenue_m'\n})\n\n# 3. Aggregate Financial & Investment Indicators (Financials)\nannual_financials = financials_df.groupby('year').agg({\n    'capex_usd_bn': 'sum',\n    'operating_margin_pct': 'mean'\n}).reset_index().rename(columns={\n    'capex_usd_bn': 'total_industry_capex',\n    'operating_margin_pct': 'avg_industry_margin'\n})\n\n# 4. Merge Macro Features into our main dataframe using the 'year' column\nenriched_df = merged_df.copy()\nenriched_df = pd.merge(enriched_df, annual_fab, on='year', how='left')\nenriched_df = pd.merge(enriched_df, annual_ai, on='year', how='left')\nenriched_df = pd.merge(enriched_df, annual_financials, on='year', how='left')\n\n# Handle any years missing data with 0 filling\nmacro_cols = ['global_wafer_capacity', 'global_ai_shipments', 'global_ai_revenue_m', 'total_industry_capex', 'avg_industry_margin']\nenriched_df[macro_cols] = enriched_df[macro_cols].fillna(0)\n\n# 5. Re-apply our sorting and feature cleanup pipeline\nenriched_df = enriched_df.sort_values(['product', 'year_month']).reset_index(drop=True)\nenriched_df['price_lag_1'] = enriched_df.groupby('product')['price'].shift(1)\nenriched_df['price_lag_2'] = enriched_df.groupby('product')['price'].shift(2)\nenriched_df['price_rolling_mean_3m'] = enriched_df.groupby('product')['price'].transform(\n    lambda x: x.shift(1).rolling(window=3, min_periods=1).mean()\n)\n\n# Clean out NaNs caused by the rolling/lag shifts\nfinal_ml_df = enriched_df.dropna(subset=['price_lag_1', 'price_lag_2']).sort_values('year_month').reset_index(drop=True)\n\nprint(\"Enriched Dataset Matrix Formed Successfully!\")\nprint(\"Current row/column dimensions:\", final_ml_df.shape)\nprint(\"\\nNew Macroeconomic Features Added:\")\nprint(final_ml_df[['year_month', 'product', 'global_wafer_capacity', 'global_ai_revenue_m', 'total_industry_capex']].head(3))\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-07-13T06:57:54.173983Z","iopub.execute_input":"2026-07-13T06:57:54.174252Z","iopub.status.idle":"2026-07-13T06:57:54.216691Z","shell.execute_reply.started":"2026-07-13T06:57:54.174230Z","shell.execute_reply":"2026-07-13T06:57:54.215627Z"}},"outputs":[{"name":"stdout","text":"Enriched Dataset Matrix Formed Successfully!\nCurrent row/column dimensions: (395, 16)\n\nNew Macroeconomic Features Added:\n  year_month        product  global_wafer_capacity  global_ai_revenue_m  \\\n0    2014-03  DRAM_DDR4_8Gb                 342705                  0.0   \n1    2014-03  NAND_64Gb_MLC                 342705                  0.0   \n2    2014-04  NAND_64Gb_MLC                 342705                  0.0   \n\n   total_industry_capex  \n0                 68.96  \n1                 68.96  \n2                 68.96  \n","output_type":"stream"}],"execution_count":23},{"cell_type":"code","source":"from sklearn.feature_extraction.text import TfidfVectorizer\nfrom sklearn.ensemble import RandomForestRegressor\nfrom sklearn.metrics import mean_absolute_error, r2_score\nimport numpy as np\nimport pandas as pd\n\n# 1. Vectorize the NLP text columns from the final enriched data\ntfidf = TfidfVectorizer(max_features=100, stop_words='english')\nX_text_sparse = tfidf.fit_transform(final_ml_df['monthly_policy_text'])\n\n# 2. One-Hot Encode the product categories\nX_product_dummies = pd.get_dummies(final_ml_df['product'], drop_first=True)\n\n# 3. Gather our numeric columns - now featuring our new macro indicators!\nnumeric_cols = [\n    'max_severity', 'price_lag_1', 'price_lag_2', 'price_rolling_mean_3m',\n    'global_wafer_capacity', 'global_ai_shipments', 'global_ai_revenue_m', \n    'total_industry_capex', 'avg_industry_margin'\n]\nX_numeric = final_ml_df[numeric_cols].values\n\n# 4. Combine all components into the ultimate feature matrix\nX_combined = np.hstack([X_text_sparse.toarray(), X_product_dummies.values, X_numeric])\ny = final_ml_df['price'].values\n\n# 5. Chronological Train-Test Split (Preserving past vs future boundary)\nsplit_idx = int(len(final_ml_df) * 0.8)\n\nX_train, X_test = X_combined[:split_idx], X_combined[split_idx:]\ny_train, y_test = y[:split_idx], y[split_idx:]\n\n# 6. Train the upgraded Random Forest Regressor\nmacro_model = RandomForestRegressor(n_estimators=100, random_state=42, n_jobs=-1)\nmacro_model.fit(X_train, y_train)\n\n# 7. Evaluate performance on the future testing window\ny_pred = macro_model.predict(X_test)\nmae = mean_absolute_error(y_test, y_pred)\nr2 = r2_score(y_test, y_pred)\n\nprint(f\"Upgraded Model Matrix Features: {X_train.shape[1]}\")\nprint(f\"Enriched Model Mean Absolute Error (MAE): ${mae:.2f}\")\nprint(f\"Enriched Model R² Score: {r2:.4f}\")\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-07-13T07:00:08.351308Z","iopub.execute_input":"2026-07-13T07:00:08.351641Z","iopub.status.idle":"2026-07-13T07:00:08.572741Z","shell.execute_reply.started":"2026-07-13T07:00:08.351615Z","shell.execute_reply":"2026-07-13T07:00:08.571773Z"}},"outputs":[{"name":"stdout","text":"Upgraded Model Matrix Features: 113\nEnriched Model Mean Absolute Error (MAE): $1151.22\nEnriched Model R² Score: 0.9766\n","output_type":"stream"}],"execution_count":24},{"cell_type":"code","source":"import ipywidgets as widgets\nfrom IPython.display import display, clear_output\nimport numpy as np\nimport pandas as pd\n\n# 1. Extract unique available products for the dropdown\nunique_products = final_ml_df['product'].unique().tolist()\n\n# 2. Build the UI Components\nhtml_header = widgets.HTML(\"<h2>Geopolitical Semiconductor Price Simulator</h2><hr>\")\nproduct_dropdown = widgets.Dropdown(options=unique_products, description='Chip Type:', value=unique_products[0])\nseverity_slider = widgets.IntSlider(value=5, min=0, max=10, step=1, description='Crisis Scale:')\nlag1_input = widgets.FloatText(value=500.0, description='Current Price ($):')\n\ntext_scenario = widgets.Textarea(\n    value='US and Netherlands announce coordinated lithography machinery export restrictions targeting advanced memory nodes.',\n    description='Policy Text:',\n    layout={'height': '100px', 'width': '80%'}\n)\n\npredict_button = widgets.Button(description='⚡ Run Simulation', button_style='success', layout={'width': '200px'})\noutput_area = widgets.Output()\n\n# 3. Dynamic Prediction Engine\ndef run_simulation(b):\n    with output_area:\n        clear_output()\n        \n        # A. Vectorize the custom written scenario text using our fitted NLP model\n        text_vector = tfidf.transform([text_scenario.value]).toarray()\n        \n        # B. Map the selected product into the exact one-hot encoded matrix layout\n        dummy_cols = list(X_product_dummies.columns)\n        product_row = pd.DataFrame(0, index=[0], columns=dummy_cols)\n        selected_prod = product_dropdown.value\n        if selected_prod in product_row.columns:\n            product_row[selected_prod] = 1\n        product_vector = product_row.values\n        \n        # C. Pull the latest known global macro values from our data to keep simulation grounded\n        latest_macro = final_ml_df[final_ml_df['year'] == final_ml_df['year'].max()].iloc[0]\n        \n        # Calculate standard lag variables realistically based on the user's manual input\n        input_price = lag1_input.value\n        lag2_mock = input_price * 0.98\n        rolling_mean_mock = (input_price + lag2_mock) / 2\n        \n        # Construct the numeric feature block array\n        numeric_vector = np.array([[\n            severity_slider.value,\n            input_price,\n            lag2_mock,\n            rolling_mean_mock,\n            latest_macro['global_wafer_capacity'],\n            latest_macro['global_ai_shipments'],\n            latest_macro['global_ai_revenue_m'],\n            latest_macro['total_industry_capex'],\n            latest_macro['avg_industry_margin']\n        ]])\n        \n        # D. Assemble the complete final matrix vector slice\n        final_input_vector = np.hstack([text_vector, product_vector, numeric_vector])\n        \n        # E. Generate forecast using the upgraded Random Forest\n        predicted_price = macro_model.predict(final_input_vector)[0]\n        \n        # F. Print out clean layout results\n        print(\"=\"*50)\n        print(f\"🔮 SIMULATED MARKET FORECAST FOR: {selected_prod}\")\n        print(\"=\"*50)\n        print(f\"» Baseline Input Price: ${input_price:,.2f}\")\n        print(f\"» Geopolitical Impact Rating: {severity_slider.value}/10\")\n        print(f\"\\n🚀 PREDICTED NEXT-MONTH MARKET PRICE: ${predicted_price:,.2f}\")\n        print(\"=\"*50)\n\n# Connect button click functionality\npredict_button.on_click(run_simulation)\n\n# Display everything in a clean stack layout\nui_layout = widgets.VBox([\n    html_header,\n    product_dropdown,\n    severity_slider,\n    lag1_input,\n    text_scenario,\n    widgets.HTML(\"<br>\"),\n    predict_button,\n    widgets.HTML(\"<br>\"),\n    output_area\n])\ndisplay(ui_layout)\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-07-13T07:02:34.290606Z","iopub.execute_input":"2026-07-13T07:02:34.290891Z","iopub.status.idle":"2026-07-13T07:02:34.321617Z","shell.execute_reply.started":"2026-07-13T07:02:34.290863Z","shell.execute_reply":"2026-07-13T07:02:34.320728Z"}},"outputs":[{"output_type":"display_data","data":{"text/plain":"VBox(children=(HTML(value='<h2>Geopolitical Semiconductor Price Simulator</h2><hr>'), Dropdown(description='Ch…","application/vnd.jupyter.widget-view+json":{"version_major":2,"version_minor":0,"model_id":"bebf231d638f48e5b35f59b650292fa9"}},"metadata":{}}],"execution_count":25},{"cell_type":"code","source":"import joblib\n\n# 1. Save the machine learning assets\njoblib.dump(macro_model, 'macro_model.pkl')\njoblib.dump(tfidf, 'tfidf.pkl')\n\n# 2. Save the explicit product categories list for our one-hot encoding system\nproduct_features = list(X_product_dummies.columns)\njoblib.dump(product_features, 'product_features.pkl')\n\n# 3. Save a small dictionary of the latest macroeconomic values to use as defaults\nlatest_year = final_ml_df['year'].max()\nlatest_macro = final_ml_df[final_ml_df['year'] == latest_year].iloc[0]\nmacro_defaults = {\n    'global_wafer_capacity': float(latest_macro['global_wafer_capacity']),\n    'global_ai_shipments': float(latest_macro['global_ai_shipments']),\n    'global_ai_revenue_m': float(latest_macro['global_ai_revenue_m']),\n    'total_industry_capex': float(latest_macro['total_industry_capex']),\n    'avg_industry_margin': float(latest_macro['avg_industry_margin']),\n    'unique_products': final_ml_df['product'].unique().tolist()\n}\njoblib.dump(macro_defaults, 'macro_defaults.pkl')\n\nprint(\"All production web assets successfully exported! Download these files from your Kaggle output directory.\")\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-07-13T07:13:02.030092Z","iopub.execute_input":"2026-07-13T07:13:02.030325Z","iopub.status.idle":"2026-07-13T07:13:02.064091Z","shell.execute_reply.started":"2026-07-13T07:13:02.030306Z","shell.execute_reply":"2026-07-13T07:13:02.063230Z"}},"outputs":[{"name":"stdout","text":"All production web assets successfully exported! Download these files from your Kaggle output directory.\n","output_type":"stream"}],"execution_count":26}]}